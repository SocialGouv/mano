# Mano

## API
- Deploy the `api` on server, with the proper environment variables for the DB

## APP 
- In `./app/src/api/index.js` change the `baseUrl` production value
- In `./app` folder, do a `yarn build:android-apk`

## DASHBOARD 
- Deploy the `dashboard` on server, with the proper variables for in config.js






